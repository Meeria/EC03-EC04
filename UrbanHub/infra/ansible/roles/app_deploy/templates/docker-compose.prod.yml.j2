services:
  backend:
    image: "{{ ecr_repository_url }}:{{ image_tag }}"
    command: ./gradlew bootRun --no-daemon
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
